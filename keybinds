fzf_personal() {
  sel=$(fd --type d --max-depth 2 . "$HOME/personal" | fzf) && [[ -n $sel ]] && cd "$sel"
  precmd
  zle reset-prompt
  nvim . && clear
}
zle -N fzf_personal

bindkey '^p' fzf_personal

